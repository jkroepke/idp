apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kratix:aggregate-to-crossplane
  labels:
    rbac.crossplane.io/aggregate-to-crossplane: "true"
rules:
  - apiGroups:
      - platform.kratix.io
    resources:
      - healthrecords
    verbs:
      - "*"
