controller:
  hostPort:
    enabled: true
    ports:
      http: 80
      https: 443
  service:
    type: ClusterIP
  admissionWebhooks:
    patch:
      enabled: false
    certManager:
      enabled: true
